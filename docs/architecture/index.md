# Architecture

The frozen M0 architecture of GoMyRobotOS: the layered model, the
partition contract and its IR, the backend model, the runtime and
recovery models, and the frozen baseline.

```{toctree}
overview
layered-model
partition-contract
ir
backend-model
runtime-model
recovery-model
frozen-baseline
```
